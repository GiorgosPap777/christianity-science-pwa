# index.json is regenerated from the mounted archive at container start,
# so the image never ships a stale copy (and stays small).
index.json

.git/
.github/
.gitignore
.env
__pycache__/
*.pyc
.DS_Store
Dockerfile
.dockerignore
compose.yaml
DOCKERHUB.md

# only needed to regenerate icons/; the generated PNGs are what ship
icon-source.jpg
